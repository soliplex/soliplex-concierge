{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Soliplex Concierge","text":"<p><code>soliplex-concierge</code> is a Soliplex extension for LLM-driven, on-demand room requests. Users chat with a self-serve \"about\" room to request access to a non-public room or the creation of a new room; the concierge files each request as a tracking issue on a Gitea repository, and an administrator resolves it.</p> <p>The documentation is organized around the two task areas:</p> <ul> <li>Rooms — the self-serve side. How the about-room   concierge collects a request and files it as an issue, and how to   set it up in an installation.</li> <li>Admin — the resolution side. How an administrator   triages the filed issues and carries out the requested   room creation or   room access.</li> </ul> <p>The extension is delivered as three Agent Skills; see Skills for where to find each one and how to install it.</p>","path":["Soliplex Concierge"],"tags":[]},{"location":"reference/api/","level":1,"title":"Python API reference","text":"<p>The public symbols of the <code>soliplex_concierge</code> package. Most users never import these directly — the room tool is wired in by configuration (see Setup) and the admin functions are driven through the <code>gitea_issues.py</code> CLI (see Administering requests). The reference is for extension authors and for scripting against the admin helpers.</p>","path":["Reference","Python API reference"],"tags":[]},{"location":"reference/api/#soliplex_conciergeconfig","level":2,"title":"<code>soliplex_concierge.config</code>","text":"<p>Tool-configuration type registered with a Soliplex installation under <code>meta.tool_configs</code>.</p> <p>Constants</p> <ul> <li><code>CGI_TOOL_NAME: str</code> — the dotted tool name,   <code>\"soliplex_concierge.tools.gitea.create_gitea_issue\"</code>.</li> <li><code>CGI_TOOL_KIND: str</code> — the tool kind (the final segment of <code>CGI_TOOL_NAME</code>).</li> <li><code>DEFAULT_PROFILE_ATTACHMENT_NAME: str</code> — default filename for the attached   user profile (<code>\"user-profile.yaml\"</code>).</li> </ul> <p><code>class CreateGiteaIssueToolConfig(soliplex.config.tools.ToolConfig)</code></p> <p>Dataclass describing the <code>create_gitea_issue</code> tool for a room. Configured keys:</p> <ul> <li><code>owner</code>, <code>repo</code> — the tracking repository (required).</li> <li><code>host</code> — Gitea base URL (default interpolation <code>\"env:GITEA_HOST\"</code>).</li> <li><code>token</code> — access token (default interpolation <code>\"secret:GITEA_ACCESS_TOKEN\"</code>).</li> <li><code>profile_attachment_name</code> — attachment filename (default   <code>DEFAULT_PROFILE_ATTACHMENT_NAME</code>).</li> <li><code>exclude_claims: list[str]</code> — profile fields to drop before attaching.</li> </ul> <p><code>owner</code> / <code>repo</code> / <code>host</code> / <code>token</code> are exposed as properties that resolve Soliplex <code>env:</code> / <code>secret:</code> interpolation markers lazily at tool-call time.</p> <ul> <li><code>from_yaml(installation_config, config_path, config_dict)</code> (classmethod) —   build the config from a room's YAML, mapping the public <code>owner</code> / <code>repo</code> /   <code>host</code> / <code>token</code> keys onto the backing fields.</li> <li><code>get_extra_parameters() -&gt; dict</code> — the configured values surfaced for tool   construction.</li> </ul>","path":["Reference","Python API reference"],"tags":[]},{"location":"reference/api/#soliplex_conciergelabels","level":2,"title":"<code>soliplex_concierge.labels</code>","text":"<p>Canonical Gitea request-label catalog, shared by the room tool and the admin helpers. Each value is a <code>{\"color\": ..., \"description\": ...}</code> mapping.</p> <ul> <li><code>ISSUE_TYPE_LABELS: dict[str, dict[str, str]]</code> — <code>new-room</code>, <code>room-access</code>.</li> <li><code>DECISION_LABELS: dict[str, dict[str, str]]</code> — <code>approved</code>, <code>denied</code>.</li> <li><code>REQUEST_LABELS: dict[str, dict[str, str]]</code> — the union of the two above.</li> </ul>","path":["Reference","Python API reference"],"tags":[]},{"location":"reference/api/#soliplex_conciergetls","level":2,"title":"<code>soliplex_concierge.tls</code>","text":"<ul> <li><code>httpx_verify()</code> — return an httpx <code>verify</code> value honoring the OS trust store   when the optional <code>truststore</code> package is installed (an   <code>ssl.SSLContext</code>), otherwise <code>True</code> (httpx's certifi default). See issue #46.</li> </ul>","path":["Reference","Python API reference"],"tags":[]},{"location":"reference/api/#soliplex_conciergetoolsgitea","level":2,"title":"<code>soliplex_concierge.tools.gitea</code>","text":"<p>The room-side tool that files a request as a Gitea issue.</p> <p><code>class CreatedGiteaIssue(pydantic.BaseModel)</code> — the created issue: <code>number: int</code>, <code>url: str</code>, <code>title: str</code>.</p> <p><code>async create_gitea_issue(ctx, title, body, request_type) -&gt; CreatedGiteaIssue</code></p> <p>The tool the about-room calls to file a request. <code>ctx</code> is the pydantic-ai <code>RunContext</code>; <code>request_type</code> is <code>\"new-room\"</code> or <code>\"room-access\"</code>. The target repository is fixed by the room's <code>CreateGiteaIssueToolConfig</code>. It ensures the type label exists, files the issue, attaches the requesting user's profile, and returns the issue number and URL.</p> <p>Exceptions</p> <ul> <li><code>UnknownRequestType(ValueError)</code> — <code>request_type</code> has no matching issue-type   label.</li> <li><code>AnonymousUser(ValueError)</code> — no user profile is attached to the run, so the   request cannot be attributed.</li> <li><code>LabelNotApplied(RuntimeError)</code> — the issue was created but its type label was   not applied (typically the Gitea account lacks Write access; see issue #59).</li> </ul>","path":["Reference","Python API reference"],"tags":[]},{"location":"reference/api/#soliplex_conciergegitea_admin","level":2,"title":"<code>soliplex_concierge.gitea_admin</code>","text":"<p>The implementation behind the admin <code>gitea_issues.py</code> CLI. Every function talks to the Gitea REST API over httpx and takes the connection as its leading arguments — <code>host</code>, <code>token</code>, <code>owner</code>, <code>repo</code> — followed by the operation's own parameters.</p> <p>Reads</p> <ul> <li><code>list_issues(host, token, owner, repo, state=\"open\", labels=None) -&gt; list[dict]</code>   — request issues (excluding pull requests), optionally filtered by <code>labels</code>.</li> <li><code>get_issue(host, token, owner, repo, number) -&gt; dict</code> — one issue's full   payload.</li> </ul> <p>Comments and state</p> <ul> <li><code>comment_issue(host, token, owner, repo, number, body) -&gt; dict</code> — add a   comment.</li> <li><code>close_issue(host, token, owner, repo, number, body=None) -&gt; dict</code> —   optionally comment, then close the issue.</li> </ul> <p>Labels</p> <ul> <li><code>list_labels(host, token, owner, repo) -&gt; list[dict]</code> — labels defined on the   repository.</li> <li><code>create_label(host, token, owner, repo, name, color, description) -&gt; dict</code> —   create a label.</li> <li><code>add_labels_to_issue(host, token, owner, repo, number, label_ids) -&gt; list[dict]</code>   — attach label ids to an issue.</li> <li><code>init_labels(host, token, owner, repo) -&gt; dict[str, str]</code> — create any missing   request labels; returns <code>{name: \"created\" | \"exists\"}</code>.</li> </ul> <p>Decisions</p> <ul> <li><code>approve_issue(host, token, owner, repo, number, body=None) -&gt; dict</code> — label   the issue <code>approved</code>, record the decision, and close it.</li> <li><code>deny_issue(host, token, owner, repo, number, body=None) -&gt; dict</code> — label the   issue <code>denied</code>, record the decision, and close it.</li> </ul> <p>Entry point</p> <ul> <li><code>main(argv=None) -&gt; int</code> — the CLI entry point used by the <code>gitea_issues.py</code>   shim.</li> </ul>","path":["Reference","Python API reference"],"tags":[]},{"location":"reference/api/#soliplex_conciergeinstaller","level":2,"title":"<code>soliplex_concierge.installer</code>","text":"<p>The implementation behind the installer skill's <code>install_concierge.py</code> shim (see Setup and the CLI reference). It wires the extension into a <code>soliplex-template</code>-generated stack through idempotent, individually-testable edits. It depends on <code>ruamel.yaml</code> and <code>soliplex-skills</code> (runtime deps of <code>soliplex-concierge</code>); most users run the shim or the <code>install-concierge</code> console script and never import these symbols.</p> <p>Entry points</p> <ul> <li><code>main(argv=None) -&gt; int</code> — the CLI entry point, exposed as the   <code>install-concierge</code> console script and delegated to by the   <code>install_concierge.py</code> shim. The skill bundle's <code>assets/</code> directory is a   required positional argument (the installed package doesn't ship the room   template); the shim prepends its own bundled path. Resolves the stack and   assets, runs <code>apply</code>, and returns a process exit code (<code>0</code> ok, <code>2</code> on   <code>InstallerError</code>).</li> <li><code>apply(stack, assets, room_skill, docs_skill, opts) -&gt; dict[str, str]</code> — make   every change idempotently against <code>stack</code>, returning a <code>{target: action}</code>   map. <code>room_skill</code> / <code>docs_skill</code> are the resolved skill trees to install (or   <code>None</code> to skip).</li> </ul> <p><code>class Options</code> (keyword-only dataclass) — one apply run's resolved options: <code>room_id</code>, <code>pin</code>, <code>gitea_host</code>, <code>gitea_token</code>, <code>owner</code>, <code>repo</code>, <code>with_truststore</code>, <code>force</code>, <code>dry_run</code>.</p> <p><code>class InstallerError(Exception)</code> — a problem applying the extension; <code>main</code> maps it to exit code <code>2</code>. Built via message-constructing classmethods (<code>not_a_stack</code>, <code>assets_missing</code>, <code>bad_skill_dir</code>, <code>bad_pyproject</code>, <code>bad_dockerfile</code>, <code>bad_installation</code>, …).</p> <p>Published skills</p> <ul> <li><code>class PublishedSkill</code> (frozen dataclass) — a skill published as a   GitHub-release tarball: <code>name</code>, <code>owner</code>, <code>repo</code>, <code>asset_tarball</code>,   <code>pointer_tag</code>, <code>dir_flag</code> (the <code>--…-skill-dir</code> flag named in download-failure   hints), <code>pointer_manifest</code>. Its <code>published</code> property adapts it to a   <code>soliplex_skills.install.PublishedSkill</code> for downloading.</li> <li><code>ROOM</code>, <code>DOCS</code> — the two <code>PublishedSkill</code> specs installed into a stack   (<code>soliplex-concierge-room</code> and <code>soliplex-docs</code>).</li> <li><code>resolve_published_skill(spec, override, version, opts, stack, ctx) -&gt; Path | None</code>   — locate a skill tree to install: the <code>override</code> local dir, else download the   published release (via <code>soliplex_skills.install.download_skill</code>, mapping its   failures to <code>InstallerError</code>) into a temp dir owned by the <code>contextlib.ExitStack</code> ctx. Returns <code>None</code> when nothing will be installed (dry run, or already   present without <code>--force</code>).</li> </ul> <p>Stack / asset discovery</p> <ul> <li><code>resolve_stack(stack_dir) -&gt; Path</code> — the resolved stack root, or raise if it   is not a generated stack (see <code>STACK_MARKERS</code>).</li> <li><code>resolve_assets(assets_dir) -&gt; Path</code> — validate and return the skill-bundle   <code>assets/</code> dir (room template + snippet).</li> <li><code>compose_project_name(stack) -&gt; str</code> / <code>default_room_id(stack) -&gt; str</code> — the   compose project name, and the default <code>about_&lt;project&gt;</code> room id.</li> </ul> <p>Edits — pure <code>(text, …) -&gt; (new_text, action)</code> functions, so <code>--dry-run</code> is just \"compute, don't write\"; <code>action</code> is <code>ADDED</code> or <code>UNCHANGED</code>:</p> <ul> <li><code>add_pyproject_dep(text, pin=None, with_truststore=False)</code> /   <code>add_dockerfile_dep(text, pin=None, with_truststore=False)</code> — add the   <code>soliplex-concierge</code> dependency to <code>backend/pyproject.toml</code> and to the   <code>backend/Dockerfile</code> <code>uv add</code> block.</li> <li><code>merge_installation(text, room_id) -&gt; (new_text, {target: action})</code> —   surgically merge the six <code>installation.yaml</code> entries.</li> <li><code>update_env(text, host, token)</code> — add the <code>GITEA_HOST</code> / <code>GITEA_ACCESS_TOKEN</code>   lines.</li> <li><code>stack_gitea_script(pin=None, with_truststore=False) -&gt; str</code> — render the   stack's <code>scripts/gitea_issues.py</code> admin shim text.</li> </ul> <p>File installs — mutate the stack, returning <code>ADDED</code> / <code>UNCHANGED</code>:</p> <ul> <li><code>install_room(assets, stack, opts)</code> — copy and rewire the about-room template.</li> <li><code>install_skill(name, skill_src, stack, opts)</code> — copy a skill tree into the   stack and defang it (via <code>soliplex_skills.install.defang_skill</code>).</li> <li><code>install_gitea_script(stack, opts)</code> — write the admin <code>gitea_issues.py</code> CLI.</li> </ul> <p>Constants — <code>ADDED</code> / <code>UNCHANGED</code> (action strings); <code>SKILL_NAME</code> and <code>STACK_MARKERS</code>; the wiring constants <code>DIST</code>, <code>TOOL_CONFIG</code>, <code>GITEA_TOOL</code>, <code>GITEA_HOST</code>, <code>GITEA_TOKEN_SECRET</code>, <code>ASSET_ROOM</code>; <code>DEFAULT_GITEA_HOST</code> / <code>DEFAULT_GITEA_TOKEN</code>; and <code>GITEA_ADMIN_MIN</code> (the <code>soliplex-concierge</code> version floor pinned into the generated <code>gitea_issues.py</code> when <code>--version</code> is omitted).</p>","path":["Reference","Python API reference"],"tags":[]},{"location":"reference/cli/","level":1,"title":"Command-line scripts","text":"<p>The repo ships two runnable CLI scripts, each bundled with a skill. Both are PEP 723 scripts: run them with <code>uv run</code>, which provisions their dependencies from the inline metadata — no install step. (Each skill also bundles a <code>skill_versions.py</code>; that one is documented with the <code>soliplex-skills</code> versions CLI, not here.)</p>","path":["Reference","Command-line scripts"],"tags":[]},{"location":"reference/cli/#install_conciergepy-install-the-extension","level":2,"title":"<code>install_concierge.py</code> — install the extension","text":"<p><code>skills/soliplex-concierge-installer/scripts/install_concierge.py</code> wires the extension into a <code>soliplex-template</code>-generated stack, idempotently. See Setup for the full walkthrough; this is the option summary.</p> <pre><code>uv run scripts/install_concierge.py --stack-dir /path/to/stack \\\n    --owner &lt;gitea-owner&gt; --repo &lt;gitea-repo&gt; [options]\n</code></pre> <p>The same logic is also installed as the <code>install-concierge</code> console script (<code>pip install soliplex-concierge</code>). It takes the options below, but — because the installed package does not ship the room template — the skill bundle's <code>assets/</code> directory is a required leading positional argument:</p> <pre><code>install-concierge /path/to/soliplex-concierge-installer/assets \\\n    --stack-dir /path/to/stack --owner &lt;gitea-owner&gt; --repo &lt;gitea-repo&gt;\n</code></pre> <p>The <code>uv run scripts/install_concierge.py</code> shim supplies that path from its own bundle automatically, so you never pass it there.</p> Option Purpose <code>--stack-dir</code> stack root (default: current directory) <code>--owner</code> / <code>--repo</code> Gitea owner and repository for filed issues <code>--gitea-host</code> / <code>--gitea-token</code> fill real <code>.env</code> values instead of placeholders <code>--version &lt;X.Y&gt;</code> pin the <code>soliplex-concierge</code> dependency added to the stack (<code>latest</code> for newest; omitted warns about skew) <code>--with-truststore</code> install the <code>[truststore]</code> extra (verify TLS against the OS trust store; issue #46) <code>--room-skill-version</code> / <code>--docs-skill-version</code> published tag to install for the room / docs skill <code>--room-skill-dir</code> / <code>--docs-skill-dir</code> install the room / docs skill from a local directory <code>--room-id</code> room id to create (default <code>about_&lt;compose-project-name&gt;</code>) <code>--force</code> overwrite an existing room/skill <code>--dry-run</code> report changes without writing","path":["Reference","Command-line scripts"],"tags":[]},{"location":"reference/cli/#gitea_issuespy-triage-and-resolve-requests","level":2,"title":"<code>gitea_issues.py</code> — triage and resolve requests","text":"<p><code>skills/soliplex-concierge-admin/scripts/gitea_issues.py</code> is a thin shim over <code>soliplex_concierge.gitea_admin</code>. The installer drops an identical copy into the stack at <code>scripts/gitea_issues.py</code>. See Administering requests for the workflow; this is the subcommand summary.</p> <p>Every subcommand takes the connection options <code>--host</code> / <code>--token</code> (falling back to <code>GITEA_HOST</code> / <code>GITEA_ACCESS_TOKEN</code>) and the required <code>--owner</code> / <code>--repo</code>.</p> <pre><code>uv run scripts/gitea_issues.py &lt;command&gt; --owner &lt;owner&gt; --repo &lt;repo&gt; [...]\n</code></pre> Command Purpose <code>init</code> create the request labels on the repository <code>list</code> list request issues (<code>--state open\\|closed\\|all</code>, repeatable <code>--label</code>) <code>search</code> filter by <code>--type new-room\\|room-access</code>, <code>--decision approved\\|denied</code>, <code>--state</code>, <code>--label</code> <code>show &lt;number&gt;</code> print one issue's title, metadata, and body <code>comment &lt;number&gt; --body &lt;text&gt;</code> add a comment <code>close &lt;number&gt; [--body &lt;text&gt;]</code> optionally comment, then close <code>approve &lt;number&gt; [--body &lt;text&gt;]</code> label <code>approved</code>, record the decision, and close <code>deny &lt;number&gt; [--body &lt;text&gt;]</code> label <code>denied</code>, record the decision, and close <p>Behind an enterprise / internal CA, add <code>--with truststore</code> to the <code>uv run</code> invocation so TLS verifies against the OS trust store (issue #46).</p>","path":["Reference","Command-line scripts"],"tags":[]},{"location":"skills/","level":1,"title":"Skills","text":"<p>The concierge is delivered as three Agent Skills, each published as a release artifact and installed either into a coding agent or into a Soliplex room. Each page below covers what the skill is for, where to find it, and where and how to install it:</p> <ul> <li><code>soliplex-concierge-installer</code> — wires   the extension into a stack; runs in a coding agent.</li> <li><code>soliplex-concierge-room</code> — the in-room   request formatter; installed into a room's configuration.</li> <li><code>soliplex-concierge-admin</code> — resolves the   filed requests; runs in a coding agent.</li> </ul>","path":["Skills"],"tags":[]},{"location":"skills/#related-skills","level":2,"title":"Related skills","text":"<p>The concierge works alongside two skills published by sibling repositories. Their own documentation covers installing and using them:</p> <ul> <li> <p><code>soliplex-template</code> —   generate and configure a Soliplex stack, create rooms, and build RAG   databases. The installer skill uses it to stand up a stack, and the   admin user uses it to provision the rooms requests ask for.</p> </li> <li> <p><code>soliplex-docs</code> — the   full Soliplex documentation as a skill. The installer wires it into the   about-room so the concierge can answer \"how do I …\" questions about Soliplex,   and the admin consults it for room, access, and RAG configuration.</p> </li> </ul> <p>Like the installed copy of the <code>soliplex-concierge-room</code> skill, the installer   strips the <code>skill_versions.py</code> helper from the copy of the <code>soliplex-docs</code>   skill it installs.  To update an installed copy, re-run the   installer's <code>install_concierge.py</code> (with <code>--force</code>) against the stack   from a coding agent.</p>","path":["Skills"],"tags":[]},{"location":"skills/soliplex-concierge-admin/","level":1,"title":"<code>soliplex-concierge-admin</code>","text":"<p>Function. Resolves the request issues the concierge files: list and filter them, follow up, carry out the operation, and close with a decision. Its <code>gitea_issues.py</code> CLI and workflow are documented under Administering requests.</p> <p>Where to find it. Published as a release artifact of <code>soliplex/soliplex-concierge</code>: the <code>admin-skill-latest</code> pointer tracks the newest build (the asset is <code>soliplex-concierge-admin-skill.tar.gz</code>), and tagged software releases (<code>v*</code>) attach the same tarball pinned to that version.</p> <p>Where to install it. Into an external coding agent (e.g. Claude Code) — the admin works against the Gitea repository and the deployed stack from there, not inside the Soliplex server. (The installer separately drops a copy of the <code>gitea_issues.py</code> CLI into the stack at <code>scripts/gitea_issues.py</code>, so the same commands can be run from the stack directory.)</p> <p>How to install it. Download and extract the release tarball into your coding agent's skills directory (where it discovers Agent Skills); the <code>soliplex-skills</code> installation guide covers fetching a published skill.</p> <p>Keeping it current. Each published copy bundles <code>scripts/skill_versions.py</code>, which can <code>list</code>, <code>diff</code>, and <code>upgrade</code> the installed skill against the published builds — see the <code>soliplex-skills</code> versions CLI docs.</p>","path":["Skills","soliplex-concierge-admin"],"tags":[]},{"location":"skills/soliplex-concierge-installer/","level":1,"title":"<code>soliplex-concierge-installer</code>","text":"<p>Function. Wires the concierge into a target Soliplex stack — creating the about-room, installing the room and docs skills, merging the <code>installation.yaml</code> entries, and dropping in the admin CLI. It is the convenient path described in Setup.</p> <p>Where to find it. Published as a release artifact of <code>soliplex/soliplex-concierge</code>: the <code>installer-skill-latest</code> pointer always tracks the newest build (the asset is <code>soliplex-concierge-installer-skill.tar.gz</code>), and every tagged software release (<code>v*</code>) attaches the same tarball pinned to that version.</p> <p>Where to install it. Into an external coding agent (e.g. Claude Code) — the skill runs there to operate on your stack, not inside the Soliplex server.</p> <p>How to install it. Download and extract the release tarball into your coding agent's skills directory (where it discovers Agent Skills); the <code>soliplex-skills</code> installation guide covers fetching a published skill. Once the agent has the skill, run its bundled <code>scripts/install_concierge.py</code> as shown in Setup.</p> <p>Keeping it current. Each published copy bundles <code>scripts/skill_versions.py</code>, which can <code>list</code>, <code>diff</code>, and <code>upgrade</code> the installed skill against the published builds — see the <code>soliplex-skills</code> versions CLI docs.</p>","path":["Skills","soliplex-concierge-installer"],"tags":[]},{"location":"skills/soliplex-concierge-room/","level":1,"title":"<code>soliplex-concierge-room</code>","text":"<p>Function. The in-room request formatter: it turns a user's new-room or room-access request into a ready-to-file tracking-issue draft that the about-room's <code>create_gitea_issue</code> tool files. See The concierge for how it fits the request flow.</p> <p>Where to find it. Published as a release artifact of <code>soliplex/soliplex-concierge</code>: the <code>room-skill-latest</code> pointer tracks the newest build (the asset is <code>soliplex-concierge-room-skill.tar.gz</code>), and tagged software releases (<code>v*</code>) attach the same tarball pinned to that version.</p> <p>Where to install it. Into a Soliplex installation, as a filesystem skill enabled in the about-room's configuration — not into a coding agent. It runs as a sub-agent of the room agent.</p> <p>How to install it. The installer skill does this for you: it downloads the release into the stack's <code>skills/</code> directory and enables it in <code>installation.yaml</code>. To wire it by hand, download the release tarball into the installation's <code>skills/</code> directory and add it to <code>filesystem_skills_paths</code> / <code>skill_configs</code> — see the manual steps in Setup.</p> <p>Keeping it current. After installing the published tarball in a coding agent, the skill contains <code>scripts/skill_versions.py</code>, which can <code>list</code>, <code>diff</code>, and <code>upgrade</code> the skill against the published builds — see the <code>soliplex-skills</code> versions CLI docs.</p> <p>When installed in a Soliplex room, however, this skill runs inside the room's agent rather than a coding agent. Room users should never reach machinery that rewrites files and calls out to GitHub/PyPI, and therefore the installer strips that helper from the copy it drops into a stack. To update an installed copy, re-run the installer's <code>install_concierge.py</code> (with <code>--force</code>) against the stack from a coding agent.</p>","path":["Skills","soliplex-concierge-room"],"tags":[]},{"location":"tasks/admin/","level":1,"title":"Administering requests","text":"<p>Requests filed by the concierge land as issues on the tracking repository. Administrators resolve them with the <code>soliplex-concierge-admin</code> skill, run from an external coding agent. The skill's <code>scripts/gitea_issues.py</code> CLI lists and filters the requests, lets you follow up on an issue, and closes it with a comment and a resolution label. The actual provisioning is delegated to the <code>soliplex-template</code> and <code>soliplex-docs</code> skills — see Room creation and Room access.</p>","path":["Tasks","Admin","Administering requests"],"tags":[]},{"location":"tasks/admin/#the-gitea_issuespy-cli","level":2,"title":"The <code>gitea_issues.py</code> CLI","text":"<p><code>scripts/gitea_issues.py</code> is a thin shim over <code>soliplex_concierge.gitea_admin</code> that talks to the Gitea REST API directly. The installer also drops an identical copy into the stack at <code>&lt;stack&gt;/scripts/gitea_issues.py</code>, so you can run the same CLI from the deployed stack directory. It is a PEP 723 script: run it with <code>uv run</code>, which provisions its <code>soliplex-concierge</code> (and <code>httpx</code>) dependency automatically — it only needs <code>uv</code> on the <code>PATH</code>.</p> <p>Provide the Gitea connection via flags or the environment:</p> <ul> <li><code>--host</code> / <code>GITEA_HOST</code> — the Gitea base URL.</li> <li><code>--token</code> / <code>GITEA_ACCESS_TOKEN</code> — a token that can comment on and close   issues (an admin token, usually more privileged than the room's filing token).</li> <li><code>--owner</code> / <code>--repo</code> — the tracking repository.</li> </ul> <p>For an enterprise / internal CA, add <code>--with truststore</code> to the <code>uv run</code> invocation so the script verifies against the OS trust store — e.g. <code>uv run --with truststore scripts/gitea_issues.py list …</code> (issue #46).</p>","path":["Tasks","Admin","Administering requests"],"tags":[]},{"location":"tasks/admin/#labels","level":2,"title":"Labels","text":"<p>The concierge tags each request with an issue-type label; the admin records the outcome with a decision label:</p> Label Kind Meaning <code>new-room</code> issue type New room request <code>room-access</code> issue type Access request for an existing room <code>approved</code> decision Room request approved <code>denied</code> decision Room request denied <p>Run <code>init</code> once per tracking repository to create them (idempotent — re-running reports each as <code>created</code> or <code>exists</code>):</p> <pre><code>uv run scripts/gitea_issues.py init --owner &lt;owner&gt; --repo &lt;repo&gt;\n</code></pre>","path":["Tasks","Admin","Administering requests"],"tags":[]},{"location":"tasks/admin/#listing-and-filtering","level":2,"title":"Listing and filtering","text":"<p><code>list</code> shows request issues; by default it lists open ones. Use <code>--state</code> (<code>open</code> / <code>closed</code> / <code>all</code>) and repeatable <code>--label</code> filters:</p> <pre><code>uv run scripts/gitea_issues.py list --owner &lt;owner&gt; --repo &lt;repo&gt;\nuv run scripts/gitea_issues.py list --state all --label new-room \\\n    --owner &lt;owner&gt; --repo &lt;repo&gt;\n</code></pre> <p><code>search</code> filters by request type and decision status directly:</p> <pre><code># open new-room requests not yet decided\nuv run scripts/gitea_issues.py search --type new-room \\\n    --owner &lt;owner&gt; --repo &lt;repo&gt;\n\n# previously approved access requests (closed)\nuv run scripts/gitea_issues.py search --type room-access \\\n    --decision approved --state closed --owner &lt;owner&gt; --repo &lt;repo&gt;\n</code></pre> <p><code>--type</code> accepts <code>new-room</code> / <code>room-access</code>; <code>--decision</code> accepts <code>approved</code> / <code>denied</code>; <code>--state</code> and <code>--label</code> work as for <code>list</code>.</p>","path":["Tasks","Admin","Administering requests"],"tags":[]},{"location":"tasks/admin/#following-up-on-an-issue","level":2,"title":"Following up on an issue","text":"<p>Read the full request — title, metadata, and body:</p> <pre><code>uv run scripts/gitea_issues.py show &lt;number&gt; --owner &lt;owner&gt; --repo &lt;repo&gt;\n</code></pre> <p>If a request is under-specified (look for <code>*(not provided)*</code> fields), ask for the missing detail with a comment and leave the issue open rather than guessing:</p> <pre><code>uv run scripts/gitea_issues.py comment &lt;number&gt; --owner &lt;owner&gt; \\\n    --repo &lt;repo&gt; --body \"Which existing room should this draw its docs from?\"\n</code></pre>","path":["Tasks","Admin","Administering requests"],"tags":[]},{"location":"tasks/admin/#closing-a-request","level":2,"title":"Closing a request","text":"<p>Carry out the operation (see Room creation / Room access), then close the issue. A plain <code>close</code> records a comment without a decision label:</p> <pre><code>uv run scripts/gitea_issues.py close &lt;number&gt; --owner &lt;owner&gt; \\\n    --repo &lt;repo&gt; --body \"Done — room 'marketing' is live.\"\n</code></pre> <p>For room-access requests, prefer the <code>approve</code> / <code>deny</code> subcommands, which also apply the <code>approved</code> / <code>denied</code> decision label — see Room access.</p> <p>Only close once the operation is actually complete. Never close a request you could not fulfil — comment what is blocking it and leave it open.</p>","path":["Tasks","Admin","Administering requests"],"tags":[]},{"location":"tasks/admin/room_access/","level":1,"title":"Resolving a room-access request","text":"<p>A room-access request (the <code>room-access</code> label) asks for an existing private room to be opened to a specific user. Resolving it means understanding who wants access to which room, then granting it with the <code>soliplex-cli room-authz</code> command set. The <code>soliplex-concierge-admin</code> skill helps you read the request and records the decision with the <code>approved</code> / <code>denied</code> label.</p>","path":["Tasks","Admin","Resolving a room-access request"],"tags":[]},{"location":"tasks/admin/room_access/#1-understand-the-request","level":2,"title":"1. Understand the request","text":"<p>Find the open access requests and read one:</p> <pre><code>uv run scripts/gitea_issues.py search --type room-access \\\n    --owner &lt;owner&gt; --repo &lt;repo&gt;\nuv run scripts/gitea_issues.py show &lt;number&gt; --owner &lt;owner&gt; --repo &lt;repo&gt;\n</code></pre> <p>The body (the room-skill's room-access template) gives the room and the justification. The requester's identity, though, comes from the attached user profile, not the body: when the concierge filed the issue, <code>create_gitea_issue</code> attached the signed-in user's profile as a YAML file (<code>user-profile.yaml</code> by default). It holds the OIDC claims Soliplex knows about the user — typically their <code>email</code> and <code>preferred_username</code> — which are exactly the discriminators you grant against below.</p> <p>Open the attachment from the issue to read the claims. (An installation may set <code>exclude_claims</code> on the tool to keep selected fields — e.g. <code>email</code> — out of the tracking repo; if the discriminator you need was excluded, fall back to the other, or ask the requester.)</p>","path":["Tasks","Admin","Resolving a room-access request"],"tags":[]},{"location":"tasks/admin/room_access/#2-grant-access","level":2,"title":"2. Grant access","text":"<p>Granting access mutates the running stack's authorization database, so run the <code>soliplex-cli room-authz</code> commands against the stack through a one-off <code>backend</code> container, from the stack directory:</p> <pre><code># Make the room private if it isn't already — add-acl-entry needs a RoomPolicy.\ndocker compose run --rm backend \\\n  /app/.venv/bin/soliplex-cli room-authz make-private /environment &lt;room_id&gt;\n\n# Allow the requesting user, matched by an OIDC claim from their profile.\ndocker compose run --rm backend \\\n  /app/.venv/bin/soliplex-cli room-authz add-acl-entry \\\n    --allow --email &lt;user-email&gt; /environment &lt;room_id&gt;\n</code></pre> <p>Notes:</p> <ul> <li><code>/environment</code> is the in-container installation path.</li> <li>Match the user with the discriminator that fits their profile: <code>--email</code> <code>&lt;email&gt;</code> or <code>--preferred-username &lt;name&gt;</code>. (<code>add-acl-entry</code> also supports   <code>--everyone</code> and <code>--authenticated</code> catch-alls, and <code>--deny</code> entries.)</li> <li>Verify the result with   <code>soliplex-cli room-authz show /environment &lt;room_id&gt;</code>.</li> </ul> <p>These access mechanics are documented in <code>soliplex-docs</code>; see the room configuration and OIDC authentication docs.</p>","path":["Tasks","Admin","Resolving a room-access request"],"tags":[]},{"location":"tasks/admin/room_access/#3-record-the-decision","level":2,"title":"3. Record the decision","text":"<p>The <code>approve</code> / <code>deny</code> subcommands close the issue and apply the matching decision label in one step.</p> <p>Approve — once the grant has actually succeeded:</p> <pre><code>uv run scripts/gitea_issues.py approve &lt;number&gt; --owner &lt;owner&gt; \\\n    --repo &lt;repo&gt; \\\n    --body \"Granted &lt;user&gt; access to '&lt;room&gt;' via room-authz add-acl-entry.\"\n</code></pre> <p>Deny — no stack change is needed; just record the decision and the reason:</p> <pre><code>uv run scripts/gitea_issues.py deny &lt;number&gt; --owner &lt;owner&gt; \\\n    --repo &lt;repo&gt; --body \"&lt;reason for denial&gt;\"\n</code></pre> <p>Never close a request you could not fulfil — if the grant fails or the request is unclear, comment what is blocking it and leave the issue open.</p>","path":["Tasks","Admin","Resolving a room-access request"],"tags":[]},{"location":"tasks/admin/room_creation/","level":1,"title":"Resolving a new-room request","text":"<p>A new-room request (the <code>new-room</code> label) asks for a room that does not yet exist. Resolving it is two jobs: read the spec the requester provided, then build the room with the <code>soliplex-template</code> skill. The <code>soliplex-concierge-admin</code> skill handles the first job and records the outcome; <code>soliplex-template</code> does the provisioning.</p>","path":["Tasks","Admin","Resolving a new-room request"],"tags":[]},{"location":"tasks/admin/room_creation/#1-read-the-spec","level":2,"title":"1. Read the spec","text":"<p>Find the open new-room requests and read one:</p> <pre><code>uv run scripts/gitea_issues.py search --type new-room \\\n    --owner &lt;owner&gt; --repo &lt;repo&gt;\nuv run scripts/gitea_issues.py show &lt;number&gt; --owner &lt;owner&gt; --repo &lt;repo&gt;\n</code></pre> <p>The body follows the room-skill's new-room template, which captures:</p> <ul> <li>Name and purpose — what the room is for.</li> <li>Knowledge sources — the documents or data it should draw from (this drives   whether a RAG database is needed).</li> <li>Access — who should be able to use it (public, or a specific set of users).</li> <li>Tools / skills — any capabilities the room should provide.</li> </ul> <p>Any field the requester left blank shows as <code>*(not provided)*</code>; comment on the issue to fill the gap rather than guessing (see Administering requests).</p>","path":["Tasks","Admin","Resolving a new-room request"],"tags":[]},{"location":"tasks/admin/room_creation/#2-create-the-room","level":2,"title":"2. Create the room","text":"<p>Use the <code>soliplex-template</code> skill against the target stack to create and configure the room. Follow <code>soliplex-docs</code> for the configuration details:</p> <ul> <li>Room configuration — create the room and set its prompt, tools, and   skills per the spec. See the   room configuration docs.</li> <li>RAG database — if the request named knowledge sources, create and ingest a   RAG database for them, then wire it into the room's <code>skill_configs</code>   (<code>rag_lancedb_stem</code>). The <code>soliplex-template</code> skill can generate and ingest   the database; see the   RAG DB docs.</li> <li>Access — if the room should be private, apply the requested access the   same way as for an access request (<code>soliplex-cli room-authz</code>).</li> </ul>","path":["Tasks","Admin","Resolving a new-room request"],"tags":[]},{"location":"tasks/admin/room_creation/#3-record-the-outcome","level":2,"title":"3. Record the outcome","text":"<p>Once the room is live, comment what you did and close the issue:</p> <pre><code>uv run scripts/gitea_issues.py close &lt;number&gt; --owner &lt;owner&gt; \\\n    --repo &lt;repo&gt; --body \"Created room 'marketing' with a RAG DB over the 2024 decks; granted the marketing team access.\"\n</code></pre> <p>If the request could not be fulfilled, comment what is blocking it and leave the issue open.</p>","path":["Tasks","Admin","Resolving a new-room request"],"tags":[]},{"location":"tasks/rooms/","level":1,"title":"The self-serve concierge","text":"<p>The concierge is a single Soliplex room — the about-room (created as <code>about_&lt;project&gt;</code> by default) — that users chat with to request changes to the installation. It answers two kinds of need:</p> <ul> <li>Questions about Soliplex — grounded in the <code>soliplex-docs</code> skill (and   optionally this installation's own RAG database).</li> <li>Room requests — a new room, or access to an existing private   room, filed as a tracking issue for an administrator to resolve.</li> </ul> <p>Filing a request is a collaboration between three actors, all behind the single chat the user sees:</p> Actor What it is Role Room agent the about-room's chat agent talks to the user, gathers details, files the issue <code>soliplex-concierge-room</code> skill a filesystem skill, run as a sub-agent formats the gathered details into an issue draft <code>create_gitea_issue</code> tool a Python tool bound to the room files the draft on the fixed tracking repository","path":["Tasks","Rooms","The self-serve concierge"],"tags":[]},{"location":"tasks/rooms/#the-request-flow","level":2,"title":"The request flow","text":"<ol> <li> <p>The user asks for a new room or for access to a private one.</p> </li> <li> <p>The room agent gathers the details with follow-up questions:</p> </li> <li>New room — proposed name and purpose, the documents or knowledge      sources it should draw from, who should have access, and any tools or      skills it should provide.</li> <li> <p>Access request — which room, and why access is needed.</p> </li> <li> <p>The room agent delegates formatting to the <code>soliplex-concierge-room</code>    skill via the <code>execute_skill</code> tool. The skill runs in isolation, so the    agent passes it everything it gathered. The skill picks the matching template    (new-room or room-access), fills every placeholder — writing <code>*(not provided)*</code>    for anything missing so the reviewer knows to follow up — and returns a draft    in a fixed shape:</p> </li> </ol> <pre><code>TYPE: &lt;new-room | room-access&gt;\nTITLE: &lt;one-line title&gt;\n\n&lt;issue body, in Markdown&gt;\n</code></pre> <p>The skill only drafts; it does not file anything itself.</p> <ol> <li>The room agent files the draft by calling <code>create_gitea_issue</code> exactly    once, passing the skill's <code>TITLE</code> as <code>title</code>, its body as <code>body</code>, and its    <code>TYPE</code> as <code>request_type</code>. The target repository is fixed by the room's tool    configuration, so the agent cannot file against an arbitrary repo. The tool:</li> <li>ensures the request-type label (<code>new-room</code> / <code>room-access</code>) exists,      creating it if needed, and tags the issue with it;</li> <li>attaches the requesting user's profile to the issue as a YAML file      (<code>user-profile.yaml</code> by default), so the requester's identity is recorded      without the agent transcribing it — see Room access;</li> <li> <p>returns the issue number and URL.</p> </li> <li> <p>The room agent reports the issue number and link back to the user. If the    tool returns an error, the agent tells the user plainly that it could not    file the request and shows the drafted title and body for manual submission —    it never claims to have filed an issue it did not create.</p> </li> </ol>","path":["Tasks","Rooms","The self-serve concierge"],"tags":[]},{"location":"tasks/rooms/#things-to-know","level":2,"title":"Things to know","text":"<ul> <li>Authentication is required. The tool attributes every request to the   signed-in user; an anonymous run is refused (<code>AnonymousUser</code>). Configure an   OIDC provider   so users are identified.</li> <li>The Gitea account needs Write access. Gitea lets a Read-only account open   issues but silently drops their labels; the tool detects the missing label and   raises rather than filing an untagged request that would slip past triage (see   issue #59). Setup covers the token requirements.</li> <li>The repository is fixed by the room config, not chosen by the agent — the   concierge can only file against the tracking repo you configured.</li> </ul> <p>To stand up the about-room in an installation, see Setup.</p>","path":["Tasks","Rooms","The self-serve concierge"],"tags":[]},{"location":"tasks/rooms/setup/","level":1,"title":"Setting up the concierge","text":"<p>Adding the concierge to an installation means creating the about-room, wiring in the <code>soliplex-concierge-room</code> and <code>soliplex-docs</code> skills, registering the <code>create_gitea_issue</code> tool, and declaring the Gitea connection. The <code>soliplex-concierge-installer</code> skill does all of this for you on a <code>soliplex-template</code>-generated stack; the manual steps below are for any other installation.</p> <p>Either way you will need a Gitea repository to file requests against, and an access token whose account has Write access to it — not just Read. Gitea lets a Read-only account open issues but silently drops their labels, so requests would be filed untagged and slip past triage (issue #59). A fine-grained token additionally needs the <code>write:issue</code> and <code>read:repository</code> scopes.</p>","path":["Tasks","Rooms","Setting up the concierge"],"tags":[]},{"location":"tasks/rooms/setup/#the-installer-skill-recommended","level":2,"title":"The installer skill (recommended)","text":"<p>Run the <code>soliplex-concierge-installer</code> skill from an external coding agent (e.g. Claude Code). It runs its bundled <code>scripts/install_concierge.py</code> with <code>uv run</code> — a thin shim that provisions <code>soliplex-concierge</code> from its PEP 723 header (so no install step is needed) and delegates to the <code>soliplex_concierge.installer</code> module — applying every change idempotently.</p> <p>Preview first with <code>--dry-run</code>, then apply:</p> <pre><code># dry run — report changes without writing\nuv run scripts/install_concierge.py --stack-dir /path/to/stack \\\n    --owner &lt;gitea-owner&gt; --repo &lt;gitea-repo&gt; --dry-run\n\n# apply, pinning the dependency version\nuv run scripts/install_concierge.py --stack-dir /path/to/stack \\\n    --owner &lt;gitea-owner&gt; --repo &lt;gitea-repo&gt; --version &lt;X.Y&gt;\n</code></pre>","path":["Tasks","Rooms","Setting up the concierge"],"tags":[]},{"location":"tasks/rooms/setup/#what-it-changes","level":3,"title":"What it changes","text":"<p>It applies these idempotent edits to the stack:</p> <ul> <li>Adds <code>soliplex-concierge</code> to <code>backend/pyproject.toml</code> and the   <code>backend/Dockerfile</code> <code>uv add</code> block (the generated Dockerfile ignores the   <code>pyproject.toml</code> deps, so both are needed).</li> <li>Merges six entries into <code>backend/environment/installation.yaml</code>:   <code>meta.tool_configs</code>, <code>environment</code>, <code>secrets</code>, two <code>skill_configs</code> — for   <code>soliplex-concierge-room</code> and <code>soliplex-docs</code> — and <code>room_paths</code>.</li> <li>Copies the about-room into <code>backend/environment/rooms/&lt;room-id&gt;/</code>   (<code>about_&lt;compose-project-name&gt;</code> by default; override with <code>--room-id</code>).</li> <li>Downloads and installs the <code>soliplex-concierge-room</code> and <code>soliplex-docs</code>   filesystem skills into <code>backend/environment/skills/</code>.</li> <li>Appends the <code>GITEA_HOST</code> / <code>GITEA_ACCESS_TOKEN</code> placeholders to <code>.env</code>.</li> <li>Writes the admin <code>scripts/gitea_issues.py</code> CLI (see   Admin).</li> </ul>","path":["Tasks","Rooms","Setting up the concierge"],"tags":[]},{"location":"tasks/rooms/setup/#useful-flags","level":3,"title":"Useful flags","text":"<ul> <li><code>--version &lt;X.Y&gt;</code> — pin the dependency added to the stack; omitting it warns   about version skew, <code>--version latest</code> opts into the newest release.</li> <li><code>--room-id</code> — room id to create (default <code>about_&lt;compose-project-name&gt;</code>).</li> <li><code>--gitea-host</code> / <code>--gitea-token</code> — fill real <code>.env</code> values instead   of placeholders.</li> <li><code>--with-truststore</code> — install the <code>soliplex-concierge[truststore]</code> extra   so the tool verifies TLS against the OS trust store rather than <code>certifi</code>'s   bundle; use when the Gitea host's certificate chains to an enterprise /   internal CA (issue #46).</li> <li><code>--room-skill-dir</code> / <code>--docs-skill-dir</code> (or <code>--room-skill-version</code> /   <code>--docs-skill-version</code>) — install the skills from a local copy or a pinned   tag instead of downloading the latest.</li> <li><code>--force</code> — overwrite an existing room/skill.</li> </ul> <p>Afterwards, set the real Gitea values in <code>.env</code> and rebuild: <code>docker compose build backend &amp;&amp; docker compose up -d</code>. Confirm both skills are present under <code>backend/environment/skills/</code> and that <code>uv run scripts/gitea_issues.py --help</code> works.</p>","path":["Tasks","Rooms","Setting up the concierge"],"tags":[]},{"location":"tasks/rooms/setup/#manual-wiring","level":2,"title":"Manual wiring","text":"<p>For a non-generated installation, do the same steps by hand.</p> <ol> <li>Install the package into the environment that runs Soliplex (it depends    on <code>soliplex</code>):</li> </ol> <pre><code>pip install -e .\n</code></pre> <p>For an enterprise / internal CA, install <code>soliplex-concierge[truststore]</code>    instead so the tool verifies TLS against the OS trust store (issue #46).</p> <ol> <li>Merge the <code>installation.yaml</code> entries from the bundled    <code>installation-snippet.yaml</code>.    They:</li> <li>register the tool-config class via <code>meta.tool_configs</code> (Soliplex resolves      the tool by its dotted <code>tool_name</code>; no core edit is needed),</li> <li>add the directories of the two filesystem skills the about-room uses      (<code>soliplex-concierge-room</code> and <code>soliplex-docs</code>) to      <code>filesystem_skills_paths</code>, and enable both under <code>skill_configs</code>, and</li> <li> <p>declare the <code>GITEA_HOST</code> environment variable and <code>GITEA_ACCESS_TOKEN</code>      secret the tool reads to call the Gitea REST API (no MCP server or external      binary is required).</p> </li> <li> <p>Copy the about-room template from    <code>rooms/about_soliplex/</code>    into your installation's <code>rooms/</code> directory, editing the <code>owner</code> / <code>repo</code> on    the <code>create_gitea_issue</code> tool to point at your tracking repository. (Either    may be a literal value or a Soliplex interpolation string such as    <code>env:GITEA_OWNER</code>.)</p> </li> <li> <p>Set the Gitea connection — <code>GITEA_HOST</code> and the <code>GITEA_ACCESS_TOKEN</code>    secret (see the <code>.env</code> lines at the bottom of the snippet), using a token    with Write access as noted above.</p> </li> </ol> <p>The about-room answers questions from the bundled <code>soliplex-docs</code> skill, so no RAG database is required. To also answer from this installation's own ingested content, uncomment the <code>skill_configs</code> block in the room's <code>room_config.yaml</code>, pointing <code>rag_lancedb_stem</code> at a database under <code>rag/db/</code>. tHe <code>soliplex-template</code> skill can generate and ingest one (see the RAG DB docs).</p>","path":["Tasks","Rooms","Setting up the concierge"],"tags":[]}]}